pipeline{
    agent any
    stages{
        stage("Git Checkout"){
            steps{
				git credentialsId: 'gitcredentials', url: 'https://github.com/dbee18/Art'
            }
        }
        
    }
}
